use algo::two_crystal_balls::two_crystal_balls;

mod algo;

// TODO: make fn which use algos for solving the same problems, and do benchs

fn main() {
    two_crystal_balls(&[false, false, false, false, false, false, false, true, true]);
}
